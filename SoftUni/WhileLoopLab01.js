function readingbooks(input) {

    let index = 0;

    let str = input[index];
    index++

    while (str !== "Stop") {
        console.log(str);



        str = input[index];
        index++


    } return;
}